{
  "name": "go-deploy-kit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "curl -H \"Content-Type:application/json\" -X POST -d '{\"script\":\"update.sh\",\"argument\":\"aaa\"}' http://localhost:8000/webhook/shell/test -i",
    "upload": "curl -F 'file=@./test.txt' http://localhost:8000/upload -v",
    "start": "env GDK_UPLOAD_DIR=./uploads/ GDK_SCRIPT_DIR=./shells/ go run main.go",
    "build": "CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build main.go",
    "pm2:start": "env GDK_UPLOAD_DIR=./uploads/ GDK_SCRIPT_DIR=./shells/ npx pm2 start main",
    "serve": "npx http-server "
  },
  "author": "",
  "license": "ISC"
}
